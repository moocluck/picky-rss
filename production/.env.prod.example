# Picky News Feed Production Configuration Template
# Copy this file to .env inside the production directory on your server and customize the values.

# Symfony app configuration
# Generate a secure random string (e.g., run: openssl rand -hex 32)
APP_SECRET=some_random_secret_string

# Telegram Bot configuration
# Your Telegram Bot API token from @BotFather
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Choose 'polling' or 'webhook' (default: polling)
TELEGRAM_MODE=polling

# If mode is 'webhook', specify the webhook secret token for security (optional)
# Generate a secure random string (e.g. run: openssl rand -hex 16)
WEBHOOK_TOKEN=

# How often (in seconds) the bot checks RSS feeds for updates (default: 900 seconds / 15 minutes)
CRON_INTERVAL=900

# Outgoing Proxy configurations (optional)
# You can use separate proxies for Telegram Bot client and feed parsers/media loaders
TELEGRAM_PROXY=
PARSER_PROXY=

# PostgreSQL database configuration
POSTGRES_DB=picky_news_feed
POSTGRES_USER=symfony
# Set a strong, secure password for the database
POSTGRES_PASSWORD=YOUR_SECURE_PASSWORD_HERE

# Database URL for Symfony (must match the database credentials above)
# "db" matches the database container service name in docker-compose.yml
DATABASE_URL=postgresql://symfony:YOUR_SECURE_PASSWORD_HERE@db:5432/picky_news_feed?serverVersion=16&charset=utf8
