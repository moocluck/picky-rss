version: '3.8'

services:
  app:
    image: ghcr.io/moocluck/picky-news-feed:latest
    container_name: picky_news_feed_app
    restart: unless-stopped
    ports:
      - "8080:80"
    env_file:
      - .env
    environment:
      APP_ENV: prod
      APP_CACHE_DIR: /tmp/picky_news_feed/cache
      APP_LOG_DIR: /tmp/picky_news_feed/log
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    container_name: picky_news_feed_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-picky_news_feed}
      POSTGRES_USER: ${POSTGRES_USER:-symfony}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
